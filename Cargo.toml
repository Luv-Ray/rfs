[package]
name = "rfs"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocopy = { version = "0.8", features = ["derive"] }
fuser = { version = "0.17", default-features = false }
libc = "0.2"
elsa = "1.10"
crc32fast = "1.4"

[dev-dependencies]
tempfile = "3.27.0"
